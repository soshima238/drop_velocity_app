import streamlit as st
import math

st.title("液滴落下速度計算ツール")

st.markdown("直径4 mmの液滴が高さ h から落下したときの衝突直前速度を求めます（空気抵抗なし）")

g = 9.80665  # 重力加速度[m/s^2]
h = st.number_input("落下高さ h [m]", min_value=0.001, max_value=10.0, value=0.15, step=0.01)

# v = sqrt(2gh)
v = math.sqrt(2 * g * h)

st.write(f"👉 落下高さ {h:.3f} m のとき、衝突速度は **{v:.2f} m/s** です。")

st.caption("※空気抵抗は無視しています。")